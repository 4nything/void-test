export interface IMetadata {
  cluster: string;
  game_length: number;
  game_start: number;
  game_start_patched: string;
  game_version: string;
  map: string;
  matchid: string;
  mode: string;
  platform: string;
  queue: string;
  region: string;
  rounds_played: number;
  season_id: string;
}